<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.futurenet.sorisoopbackend.payment.domain.PaymentRepository">

    <select id="getSubscriptionByMemberId"
            parameterType="long"
            resultType="com.futurenet.sorisoopbackend.payment.dto.response.SubscriptionResponse">
        SELECT S.STATUS,
               P.TYPE,
               S.NEXT_BILLING_AT,
               S.CANCELLED_AT
        FROM SUBSCRIPTION S
                 JOIN SUBSCRIPTION_PLAN P ON S.SUBSCRIPTION_PLAN_ID = P.ID
        WHERE S.MEMBER_ID = #{memberId}
    </select>

</mapper>

